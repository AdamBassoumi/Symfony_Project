{% extends 'base.html.twig' %}

{% block body %}
    <div class="category-container">
        <h1 class="category-title">Create New Category</h1>

        <form action="{{ path('app_category_new') }}" method="POST" class="category-form">
            <div class="form-group">
                <label for="name">Category Name</label>
                <input type="text" name="name" id="name" class="category-form-input" required />
            </div>

            <button type="submit" class="category-submit-btn">
                <i class="fas fa-check"></i> Submit
            </button>
        </form>

        <a href="{{ path('app_category_index') }}" class="category-back-btn">
            <i class="fas fa-arrow-left"></i> Back to Categories
        </a>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/styles/category.css') }}">
{% endblock %}
