{% extends 'base.html.twig' %}

{% block title %}Utilisateur Index{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/styles/utilisateur.css') }}">
{% endblock %}

{% block body %}
    <div class="utilisateur-container">
        <h1 class="utilisateur-title">Utilisateur Index</h1>
        <table class="utilisateur-table">
            <thead>
                <tr>
                    <th>Id</th><th>Nom</th><th>Prénom</th><th>Email</th><th>Mdp</th><th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for utilisateur in utilisateurs %}
                    <tr>
                        <td>{{ utilisateur.id }}</td>
                        <td>{{ utilisateur.nom }}</td>
                        <td>{{ utilisateur.prenom }}</td>
                        <td>{{ utilisateur.email }}</td>
                        <td>{{ utilisateur.mdp }}</td>
                        <td>
                            <a href="{{ path('app_utilisateur_show', {'id': utilisateur.id}) }}" class="utilisateur-btn">
                                <i class="fas fa-eye"></i> Show
                            </a>
                            <a href="{{ path('app_utilisateur_edit', {'id': utilisateur.id}) }}" class="utilisateur-btn">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a href="{{ path('app_utilisateur_delete', {'id': utilisateur.id}) }}" class="utilisateur-delete-btn">
                                <i class="fas fa-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="utilisateur-btn-group">
            <a href="{{ path('app_utilisateur_new') }}" class="utilisateur-create-btn">
                <i class="fas fa-plus-circle"></i> Add Utilisateur
            </a>
        </div>
    </div>
{% endblock %}
