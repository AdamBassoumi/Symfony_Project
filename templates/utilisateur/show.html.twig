{% extends 'base.html.twig' %}

{% block title %}Utilisateur Details{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/styles/utilisateur.css') }}">
{% endblock %}

{% block body %}
    <div class="utilisateur-container">
        <h1 class="utilisateur-title">Utilisateur Details</h1>

        <div class="utilisateur-details-wrapper">
            <table class="utilisateur-table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ utilisateur.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ utilisateur.nom }}</td>
                    </tr>
                    <tr>
                        <th>Prénom</th>
                        <td>{{ utilisateur.prenom }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ utilisateur.email }}</td>
                    </tr>
                    <tr>
                        <th>Mot de passe</th>
                        <td>{{ utilisateur.mdp }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="utilisateur-btn-group">
            <a href="{{ path('app_utilisateur_index') }}" class="utilisateur-back-btn">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
            <a href="{{ path('app_utilisateur_edit', {'id': utilisateur.id}) }}" class="utilisateur-btn">
                <i class="fas fa-edit"></i> Edit
            </a>
        </div>

        {{ include('utilisateur/_delete_form.html.twig', {'class': 'utilisateur-delete-form'}) }}
    </div>
{% endblock %}
